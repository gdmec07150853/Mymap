apply plugin: 'com.android.application'

android {
    signingConfigs {
        charies {
            keyAlias 'charies'
            keyPassword 'charies'
            storeFile file('C:/Users/zhu/AndroidStudioProjects/Mymap/BaiduLBS_AndroidSDK_Lib (1)/libs/x86_64/libBaiduMapSDK_base_v4_1_1.so')
            storePassword 'charies'
        }
    }
    compileSdkVersion 24
    buildToolsVersion "24.0.2"
    defaultConfig {
        applicationId "cn.edu.gdmec.s07150853.mymap"
        minSdkVersion 15
        targetSdkVersion 24
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            signingConfig signingConfigs.charies
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:24.2.0'
}
